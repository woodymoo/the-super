[pytest]
# Put the repo root on sys.path so `pytest` works from the project root the same
# way `python -m pytest` does. Without this, bare `pytest` fails with
# ModuleNotFoundError: the_super — which is the command the manual tells you to run.
pythonpath = .
testpaths = tests
